<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="theme-color" content="#37474f" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="format-detection" content="telephone=no" />
    <title>
      아즈인포 - <%- locals.title? title : '' %>
    </title>
    <meta name="viewport" content="width=device-width" />

    <link rel="stylesheet" href="<%- static %>/css/main.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:200,300,500,700" />
    <script>
window.BaseURI = '<%- baseuri %>'
<% if(locals.session) { %>
window.session = '<%- session %>'
<% } %>
    </script>

    <script src="<%- static %>/js/lib.js"> </script>
    <% if(locals.css) { %>
      <% for(var i in css) { %>
        <link rel="stylesheet" href="<%- static %><%- css[i] %>" />
      <% } %>
    <% } %>
    <% if(locals.script) { %>
      <% for(var i in script) { %>
        <script src="<%- static %><%- script[i] %>"> </script>
      <% } %>
    <% } %>
  </head>
  <body id="body">
    <% if(!(locals.noHamburger && noHamburger)) { %>
      <input type="radio" name="aside" id="menu-toggle" class="hidden" />
      <input type="radio" name="aside" id="reset" class="hidden" />
    <% } %>
    <div class="toolbar-decorator"> </div>
